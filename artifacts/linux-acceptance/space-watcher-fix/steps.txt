1. Reproduced the old Release from the full /mnt/d/ccsmv2-linux root.
2. Captured the renderer at Opening Space and inspected open process handles.
3. Observed recursive traversal inside node_modules/.pnpm and eventual recovery after about five minutes.
4. Changed every platform to a shallow root watch with bounded materialized scopes.
5. Added watcher adapter and backend scope-routing regressions.
6. Built an embedded debug desktop and cold-started it from the same full repository root.
7. Created lib.user-is-merged with /mnt/d/ccsmv2-linux as its root.
8. Switched to desktop and back to lib.user-is-merged twice.
9. Verified ready UI, visible Browser, stable runtime identity, SQLite state, process handles, and clean screenshots.
10. Ran Rust workspace, TypeScript, frontend, formatting, and diff gates.
11. Built the Ubuntu archive from fix commit f46345a and extracted it to a fresh directory.
12. Repeated full-root cold start, create, switch back, and switch again against the extracted Release binary.
13. Verified the archive and binary SHA-256 values, final SQLite state, zero node_modules handles, and clean Release screenshots.
